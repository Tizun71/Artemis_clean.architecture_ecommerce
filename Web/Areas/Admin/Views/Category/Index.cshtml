@model IEnumerable<Category>

@{
    ViewBag.Title = "Quản lý danh mục";
}

<div class="box box-primary">
    <div class="box-body">
        <form id="formSearch" action="" method="get">
            <div class="input-group">
                <input type="hidden" name="" value="" />

                <input name="searchValue"
                       type="text"
                       class="form-control"
                       placeholder="Nhập tên danh mục cần tìm"
                       value=""
                       autofocus />

                <div class="input-group-btn">
                    <button class="btn btn-info" type="submit">
                        <i class="fa fa-search"><</i>
                    </button>

                    <a asp-controller="Product" asp-action="Create" class="btn btn-primary" style="margin-left:5px">
                        <i class="fa fa-plus"></i> Bổ sung
                    </a>
                </div>
            </div>
        </form>

        <div>
            <p style="margin-top:10px">
                có <strong></strong> loại hàng trong tổng số <strong></strong> trang
            </p>
            <div class="table-responsive">
                <table class="table table-bordered table-hover table-striped">
                    <thead>
                        <tr class="bg-primary">
                            <th>Tên danh mục</th>
                            <th style="width:100px"></th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.CategoryName</td>
                                <td class="text-right">
                                    <a href="" class="btn btn-xs btn-info" style="background-color: gray">
                                        <i class="fa fa-eye"></i>
                                    </a>
                                    <a asp-controller="Category" asp-action="Update" asp-route-id="@item.CategoryID" class="btn btn-xs btn-info">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                    <a asp-controller="Product" asp-action="Delete" class="btn btn-xs btn-danger">
                                        <i class="fa fa-remove"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
